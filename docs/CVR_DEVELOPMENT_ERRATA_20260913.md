# `results/cvr/development_report.md` 勘误

原报告保留为提交 `1509251` 对当时实验的历史记录，不改写原文件。经实现和口径复核，以下结论需要更正：

- `SEGFIXED` 并非 ISR 严格对照；修复后的严格对照臂名为 `COMPAT`。
- 原实现规划深度实际为 1，且没有执行定位中断，不能称为多步联合调度。
- 原 300 个决策日志中有 100 个 ISR 空文件，因此“300/300 独立审计有效”不成立。
- `physical_stations_removed` 和 `restored_legacy_stations` 是计划/回退计数代理，不能解释为实际物理停点净减少。
- “交叉测向损失是唯一退化原因”和“覆盖余量意味着接近性能极限”均超出实验能支持的范围。

修复后的事实、逐动作证据和采用结论以 `docs/CVR_AUDIT_FIX_REPORT_20260913.md` 为准。

