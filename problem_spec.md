# B题 problem_spec（依据正式赛题、附件1、附件2整理）

## 1. 任务定义

在半径1800m的目标圆域内，存在10-16个未知干扰源，频道互不相同，频道编号1-20。
每个源有效接收半径 R ∈ [1000,1500]m。全向源360°可接收；定向源只在方向 n 两侧各90°内可接收。
测向机示向度误差 |e| <= 1°，同一地点电磁环境固定，重复测量不能平均消除误差。

机器狗从(0,0)出发，初始频道1，速度5m/s，必须串行调用 /enter、/measure、/clear、/exit。
目标：确保全部源被清除，并最小化虚拟总时间。

## 2. 官方信息边界

接口不返回：源数量、真实位置、有效接收半径、源类型、定向方向。
接口可返回：
- /measure: no_signal | near | direction + svd_deg
- /clear: no_target_in_range | success
- /enter: remaining_real_duration_s 等现实时间信息
- /exit: user_exit

## 3. 时间成本表

| 动作 | 移动 | 换频 | 固定动作 | 备注 |
| --- | --- | --- | --- | --- |
| /measure | 距离/5 s | 与当前测向频道不同时1s | 5s | 成功后当前频道变为本次 channel |
| /clear | 距离/5 s | 0 | 3s / 5s | 不改变测向频道；成功与否仅看20m |
| /enter | 0 | 0 | 0 | 开始虚拟与现实计时 |
| /exit | 0 | 0 | 0 | 主动结束 |

总虚拟时间：
T = L/5 + N_sw + 5 N_measure + 3 N_fail + 5 N_success。

## 4. 问题一公式

对 s_i, θ_i，u_i=(cosθ_i,sinθ_i)，v_i=(-sinθ_i,cosθ_i)：
- u_i^T(z-s_i) >= 0
- |v_i^T(z-s_i)| <= tan1° u_i^T(z-s_i)

P = ∩ W_i；D(P)=max_{a,b in V(P)}||a-b||；r*=min_q max_{z in P}||q-z||。
一次清除保证：r* <= 20m；工程实现取19.5m。

## 5. 问题二公式

F = D ∩ W_1 ∩ B(s_1,1500)
Q_vis = { q : max_{z in F} ||q-z|| <= 1000 }
q± = s_1 + 750u_1 ± 300v_1
统一最坏距离上界 817.65m < 1000m。

## 6. 问题三覆盖

S3 = {原点} ∪ 半径1200m正六边形6顶点，共7点。
任意全向源到某点距离 <= 968.90m < 1000m。

## 7. 问题四覆盖

三角网格边长 h=950m，S4 = {s_ij: ||s_ij|| <= 1800+h}，共31点。
任意源位于某网格三角形内，至少一个顶点在任意180°定向可见半平面内且距离<=950m。

## 8. 单源保底

一次有效示向后，矩形半宽 <=1500 sin1° < 26.18m。
122点：q_{k,±}=s+25k u±13.1v, k=0..60。
最坏距离 sqrt(12.5^2+13.1^2)≈18.11m<20m，最多122次 clear 必成功。

## 9. 完成证书

满足以下之一才允许 /exit：
1. 已成功清除16个不同频道；
2. 20个频道均为已清除或已由覆盖证明判定不存在。

## 10. 方案B质量 Gate

- 半平面方向、0°/360°环绕、空集、退化案例均已测试；
- 旋转卡壳直径与暴力顶点对交叉验证；
- no_signal 不直接删除目标假设；
- 所有时间成本包含移动、检测、换频、清除；
- 本地随机实验与官方正式测试严格区分；
- 训练集/测试集种子独立；
- 正式运行冻结权重，异常时由A0接管。